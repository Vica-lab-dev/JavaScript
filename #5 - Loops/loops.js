/**let i = 0 => odakle krece petlja.
 * i<100 => uslov, do kada ce petlja da radi.
 * i++ => nakon svakog ponavljanja povecaj i za 1...
 */

for(let i= 0; i < 100; i++)
{
    console.log(i);
}

let cars = ["bmw", "audi", "zastava", "mercedes", "amg", "honda", "toyota"];
for(let i= 0; i < cars.length; i++)
{
    console.log(cars[i]);
}